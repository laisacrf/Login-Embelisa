@model CondominioApp.Models.ContaMensal
@{
    ViewData["Title"] = "Pagamento PIX";
}

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

<div class="container" style="font-family:'Poppins',sans-serif; max-width:600px; margin:50px auto; background:#fff; padding:30px; border-radius:12px; box-shadow:0 0 15px rgba(0,0,0,.1); text-align:center;">
    <h1>💳 Pagamento PIX</h1>

    <div class="info" style="margin:20px 0; text-align:left;">
        <p><strong>Descrição:</strong> @Model.Descricao</p>
        <p><strong>Valor:</strong> R$ @ViewBag.ValorPix</p>
        <p><strong>Vencimento:</strong> @Model.DataVencimento.ToShortDateString()</p>
        <p><strong>Status:</strong> @(Model.Pago ? "✅ Pago" : "⏳ Pendente")</p>
    </div>

    <div class="qr-box" style="margin:25px auto;">
        <h3>Escaneie o QR Code abaixo:</h3>
        <img src="data:image/png;base64,@ViewBag.QRCodeBase64" alt="QR Code PIX" style="width:250px; margin-top:15px; border:2px solid #ccc; border-radius:10px;" />
        <p style="margin-top:10px; font-size:14px;">Ou copie o código PIX abaixo:</p>
        <textarea id="pixCodigo" readonly style="width:100%; height:100px; border-radius:8px; border:1px solid #ddd; padding:10px;">@Model.PixCodigo</textarea>
        <button onclick="copiarPix()" class="btn" style="margin-top:10px; background:#007bff; color:white; border:none; padding:10px 20px; border-radius:8px; cursor:pointer;">Copiar Código PIX</button>
    </div>

    <div style="margin-top:25px;">
        <a href="@Url.Action("Index", "Conta")" class="btn" style="background:#28a745; color:white; text-decoration:none; padding:10px 20px; border-radius:8px;">Voltar às Contas</a>
    </div>
</div>

<script>
    function copiarPix() {
        const codigo = document.getElementById("pixCodigo");
        codigo.select();
        document.execCommand("copy");
        alert("✅ Código PIX copiado!");
    }
</script>
